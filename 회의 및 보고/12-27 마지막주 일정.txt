-------------------------------------------------------------------------------------------------------------------------------------------
~12. 29
1) del.php view파일을 만들어서 ajax로 코드 작성 --------------------------------------------------------------------------------------
2) pagination코드를 process파일로 옮겨서 view파일에 보이지 않게끔 작성-------------------------------------------------------------
3) db_open후 마지막에 que_close를 반드시 작성할 것---------------------------------------------------------------------------------
4) 삭제, 수정, 추가시에 alert를 반드시 띄울 것------------------------------------------------------------------------------------------
5) edit 페이지에 파일목록 출력시 각 파일마다 삭제버튼을 구현 (해당파일의 id를 불러오는데 까지 성공)----------------------
-------------------------------------------------------------------------------------------------------------------------------------------
LEFT INNER RIGHT 를 언제 사용해야하는지 이유를 알아야함
INNER조인은 교집합 즉, t_file테이블에 filename이 없으면 보여지지 않음. (NULL값 일 경우)
LEFT는 t_file에 filename이 없어도 출력 따라서 LEFT JOIN을 이용하여 게시판 목록 전부 출력
-------------------------------------------------------------------------------------------------------------------------------------------
12월 30 ~ 12월 31
rms DB SELECT 문제
Q) 엄태영의 본부, 팀, 직급, 직책을 SELECT해라 (직원테이블과, commons2 를 join하여)
A)
SELECT 
	DISTINCT 
			em_nm AS 이름
			, (SELECT kr_name FROM t_common2 WHERE t_common2.cm2_seq = POSITION1) AS 직급
			, (SELECT kr_name FROM t_common2 WHERE t_common2.cm2_seq =  division1) AS 부서
			, (SELECT kr_name FROM t_common2 WHERE t_common2.cm2_seq =  division2) AS 팀
			 FROM t_employee
			LEFT JOIN t_common2
			ON t_employee.em_seq = t_common2.cm1_seq
			WHERE em_nm = '엄태영' ;
-------------------------------------------------------------------------------------------------------------------------------------------
CMS소스코드분석 후 브리핑
SUMMARY와 기간별 리포트 페이지를 분석해야함

SQLD자격증 시험 1월 접수 예정

